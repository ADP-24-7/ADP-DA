name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - feature/**

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ADP-DA

    steps:
      - uses: actions/checkout@v4
        with:
          path: ADP-DA

      - name: Checkout BE Compose and E1-E3 governance contract
        uses: actions/checkout@v4
        with:
          repository: nahee8034-ux/ADP-BE
          # Pin the matching producer contract while the baseline is not on upstream main.
          ref: 50a1f8bc719708cfdcb4e041672f9aead88e0a0a
          path: ADP-BE

      - name: Checkout FE E1-E3 governance projection
        uses: actions/checkout@v4
        with:
          repository: ADP-24-7/ADP-FE
          ref: 6a329e0dc94d067051fdc1ae1754a3e34e7d5380
          path: ADP-FE

      - name: Validate Docker Compose
        run: |
          mkdir -p ../ADP-FE ../ADP-Docs
          docker compose \
            --project-directory ../ADP-BE \
            -f ../ADP-BE/docker-compose.yml \
            -f "$PWD/docker-compose.yml" \
            --profile test \
            config --quiet

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"

      - name: Lint
        run: python -m ruff check .

      - name: Typecheck
        run: python -m mypy 02_ai/src

      - name: Contract check
        run: python scripts/validate_contracts.py

      - name: Test
        run: python -m pytest 02_ai/tests

      - name: Restore BE P0 contract for Digital Asset tests
        uses: actions/checkout@v4
        with:
          repository: nahee8034-ux/ADP-BE
          ref: 3692e572da1ff09b040a3e80a863030282060c58
          path: ADP-BE

      - name: Digital Asset Test
        run: python -m pytest 03_digital_asset/tests
